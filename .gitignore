# Local-machine bundled assets — too big for plain git. Regenerate with
# ./bundle.sh on a machine with internet. If you want to vendor these for
# air-gapped turnup, enable Git LFS (.gitattributes already lists the patterns)
# and remove this line.
assets/

# Local docker compose overrides.
docker-compose.override.yml
